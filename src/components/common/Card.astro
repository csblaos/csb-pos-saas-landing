---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`bg-white border-2 border-black shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] p-6 ${className || ''}`}>
  <slot />
</div>
